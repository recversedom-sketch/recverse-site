---
import BaseLayout from "../layouts/BaseLayout.astro";

const starters = [
  {
    name: "Website Sprint (1 Woche)",
    price: "ab CHF 1'200",
    desc: "Landing oder Mini-Business-Site mit sauberem Deploy. Ideal, wenn du schnell live willst.",
    includes: ["Scope-Call (45 Min)", "Struktur + Landing", "Deploy + Domain-Setup", "SEO/Performance Basics"],
  },
  {
    name: "KI-PoC Sprint (5-10 Tage)",
    price: "ab CHF 1'900",
    desc: "Risikoklarheit vor Code: Datenfluss, Risiken, Architektur, PoC-Demo.",
    includes: ["Use-Case & Datenfluss", "Risiko- und Compliance-Check (light)", "PoC-Demo", "Entscheidungsdokument"],
  },
  {
    name: "Prozess-Quickscan (2-5 Tage)",
    price: "ab CHF 1'500",
    desc: "Ist/Soll, Pain Points, KPI-Set, Umsetzungsplan. Kein Blabla.",
    includes: ["Interviews/Workshop", "Ist/Soll Map", "KPI-Vorschlag", "Roadmap (90 Tage)"],
  },
];

const addons = [
  { name: "Blog-Setup (Markdown)", price: "CHF 300-900" },
  { name: "Zusaetzliche Seiten (pro Seite)", price: "CHF 120-300" },
  { name: "Copy-Polish (bestehender Text)", price: "CHF 250-800" },
  { name: "Consent/Tracking Setup (light)", price: "CHF 200-900" },
  { name: "Monatliche Pflege/Retainer", price: "CHF 1'000-4'000/Monat" },
];
---

<BaseLayout title="Pakete - RecVerse" description="Anfragbare Pakete und Add-ons: Website, KI-PoC, Prozess-Quickscan.">
  <section class="mx-auto max-w-6xl px-4 py-16">
    <h1 class="text-3xl md:text-4xl font-semibold">Pakete</h1>
    <p class="mt-4 text-zinc-300 max-w-3xl">
      Das ist kein klassischer Checkout-Shop. Du waehlst ein Paket und Add-ons, ich antworte mit einem klaren Angebot (Scope, Preis, Timeline).
    </p>

    <div class="mt-10 grid gap-6 md:grid-cols-3">
      {starters.map((p) => (
        <div class="rounded-3xl border border-zinc-800/60 bg-zinc-900/20 p-8 flex flex-col">
          <div class="text-xl font-semibold">{p.name}</div>
          <div class="mt-2 text-sm text-zinc-300">{p.price}</div>
          <p class="mt-4 text-sm text-zinc-400">{p.desc}</p>

          <ul class="mt-6 space-y-2 text-sm text-zinc-300">
            {p.includes.map((x) => <li>- {x}</li>)}
          </ul>

          <div class="mt-8">
            <a
              href={"/contact?package=" + encodeURIComponent(p.name)}
              class="inline-flex w-full items-center justify-center rounded-xl bg-zinc-100 px-5 py-3 text-sm font-semibold text-zinc-900 hover:bg-zinc-200 transition"
            >
              Angebot zu {p.name} anfragen
            </a>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-12 rounded-3xl border border-zinc-800/60 bg-zinc-950/40 p-8">
      <h2 class="text-2xl font-semibold">Add-ons</h2>
      <p class="mt-3 text-sm text-zinc-400">Du kannst Add-ons beim Anfrageformular auswaehlen oder einfach reinschreiben.</p>

      <div class="mt-6 grid gap-3 md:grid-cols-2">
        {addons.map((a) => (
          <div class="rounded-2xl border border-zinc-800/60 bg-zinc-900/20 p-5 flex items-center justify-between gap-4">
            <div class="text-sm text-zinc-200">{a.name}</div>
            <div class="text-sm text-zinc-400 whitespace-nowrap">{a.price}</div>
          </div>
        ))}
      </div>

      <div class="mt-8">
        <a
          href="/contact"
          class="inline-flex items-center justify-center rounded-xl border border-zinc-700 px-5 py-3 text-sm font-semibold text-zinc-100 hover:border-zinc-500 transition"
        >
          Individuelle Anfrage
        </a>
      </div>
    </div>

    <div class="mt-10 text-xs text-zinc-500">
      Hinweis: Wenn spaeter echter Checkout geplant ist, brauchst du u. a. klare AGB, Preis-/Steuer-Logik, ggf. Widerrufs-Handling (B2C) und saubere Zahlungs-/Retourenprozesse.
    </div>
  </section>
</BaseLayout>
